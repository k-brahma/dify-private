version: '3.8'

services:
  # Disable the built-in nginx service
  nginx:
    profiles:
      - disabled
  
  # Configure web service for nginx-proxy
  web:
    build:
      context: ../web
      dockerfile: Dockerfile
    expose:
      - "3000"
    networks:
      - dify-network
      - default
  
  # Configure api service for nginx-proxy  
  api:
    build:
      context: ../api
      dockerfile: Dockerfile
    expose:
      - "5001"
    networks:
      - dify-network
      - default
  
  # Configure worker service
  worker:
    build:
      context: ../api
      dockerfile: Dockerfile
    networks:
      - dify-network
      - default
      
  # Configure plugin daemon for nginx-proxy
  plugin_daemon:
    expose:
      - "5002"
    networks:
      - dify-network
      - default

networks:
  dify-network:
    external: true